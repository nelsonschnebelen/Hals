# Copy to .env.local (gitignored) and fill in the secrets.

# Cloudinary — cloud name + API key are non-secret (safe as inline fallbacks).
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=dpjkoevbm
CLOUDINARY_API_KEY=622749214288621
# Rotate before use — this was shared in chat. Server-only.
CLOUDINARY_API_SECRET=

# fal.ai — server-only. Rotate before use.
FAL_KEY=

# Public site URL — sets metadataBase and silences the OG warning.
NEXT_PUBLIC_SITE_URL=https://halsthesteakhouse.com
